<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Movies:</strong>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %> | About/Quote: <%= bookmark.movie.overview %> <%= link_to 'Delete Bookmark', bookmark_path(bookmark), method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-secondary' %></li>
  <% end %>
</p>


<%= link_to 'Back', lists_path, class: 'btn btn-info' %>
<%= link_to 'Add Bookmark', new_list_bookmark_path(@list), class: 'btn btn-primary' %>
